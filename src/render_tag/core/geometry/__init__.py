"""Pure-Python/NumPy geometry primitives shared across the render-tag stack.

No Blender or render_tag-domain imports — this is foundation-layer code that
sits below schema, generation, backend, audit, viz, and orchestration.
"""

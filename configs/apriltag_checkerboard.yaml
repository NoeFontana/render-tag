# render-tag: AprilTag ChArUco (cb) Calibration Baseline
# Optimized for high-precision corner extraction.

dataset:
  output_dir: output/apriltag_cb
  num_scenes: 1
  seeds:
    global_seed: 42
  evaluation_scopes: [calibration, corner_accuracy]

camera:
  resolution: [1920, 1080]
  fov: 65.0
  samples_per_scene: 10
  
  # Closer range for calibration boards
  min_distance: 0.4
  max_distance: 1.5
  min_elevation: 0.3
  max_elevation: 0.9

tag:
  family: tag36h11
  size_meters: 0.08
  material:
    randomize: false  # Calibration usually uses matte boards
    roughness_min: 0.8
    specular_min: 0.1

scenario:
  layout: cb
  tag_families: [tag36h11]
  # 6x6 grid of cells, 18 tags total (checkerboard)
  grid_size: [6, 6]
  square_size: 0.12
  marker_margin: 0.01
  sampling_mode: random

scene:
  lighting:
    intensity_min: 200
    intensity_max: 600
    radius_min: 0.2
    radius_max: 0.4
  texture_dir: assets/textures/background

physics:
  drop_height: 0.05
